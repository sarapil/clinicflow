# ClinicFlow — Entity Relationship Diagram
# كلينك فلو — مخطط العلاقات

> 8 DocTypes

```mermaid
erDiagram
    CFSettings ||--|| CFSettings : singleton
```

> **Note:** This is a placeholder ERD. Update with actual DocType relationships from the JSON definitions.
> Run: `ls clinicflow/clinicflow/*/doctype/` to discover all DocTypes and their Link fields.
